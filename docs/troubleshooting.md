# Troubleshooting (placeholder)

Add notes as you hit issues:

- ManiSkill2 install + dependencies
- rendering / headless issues
- GPU / CUDA mismatch
- performance tips (image size, num envs, frame skip)


